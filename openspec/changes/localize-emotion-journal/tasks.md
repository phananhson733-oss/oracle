# Tasks: 情绪日记本土化

## Task 1: 扩展情绪模型与前端数据层
**状态**：待执行
**依赖**：无

### 步骤
1. 在 `cbt.js` 中将 MOODS 常量从 4 项扩展为 12 项，按色调分组
2. 每个情绪包含 `id`、`label`、`group`（暖/冷/重/轻）、`color`、`desc`（意境短语）
3. 支持多选情绪（`record.moods` 改为数组）
4. 新增可选 `bodySignal` 字段

### 验证
- [ ] 12 个情绪项可正确渲染
- [ ] 支持同时选择多个情绪
- [ ] `finishAnalysis` 发送的 `moods` 为数组格式，与后端 API 兼容

---

## Task 2: 重构引导流程（5 步 → 3 步）
**状态**：待执行
**依赖**：Task 1

### 步骤
1. 修改 `cbt.wxml` 引导部分：
   - Step 0: 「今天遇到了什么事？」+ 情绪选择（合并旧 step 0+1）
   - Step 1: 「脑子里第一个蹦出来的念头是什么？」（合并旧 step 2+3 的自动思维和 Hot Thought）
   - Step 2: 「如果安慰最好的朋友，你会怎么说？」（合并旧 step 3+4 的证据辩论和平衡思维）
2. 更新 `STEPS` 常量为 `['记录', '觉察', '转念']`
3. 更新步骤进度指示器
4. 更新 `isStepValid` 验证逻辑
5. 本土化所有 placeholder 和提示文案

### 验证
- [ ] 3 步流程可正常走完
- [ ] 步骤进度显示正确（1/3, 2/3, 3/3）
- [ ] 取消/回退逻辑正常
- [ ] 完成后进入分析页面

---

## Task 3: 情绪选择器 UI 重设计
**状态**：待执行
**依赖**：Task 1
**可并行**：与 Task 2 并行

### 步骤
1. 在 `cbt.wxml` 中设计新的情绪选择器：
   - 先展示 4 个色调大区（暖/冷/重/轻），每个用标志色标识
   - 点击色调后展开该组 3 个具体情绪词
   - 选中的情绪高亮，支持多选
2. 在 `cbt.wxss` 中添加对应样式
3. 强度滑块保留，但标签从「强度」改为「有多强烈？」

### 验证
- [ ] 色调分组交互正常
- [ ] 多选情绪在 UI 上有明确的选中状态
- [ ] 选中情绪在日历 mood-dot 上使用对应色调
- [ ] 水墨风设计语言保持一致

---

## Task 4: 本土化 AI 分析 Prompt
**状态**：待执行
**依赖**：无（可与前端任务并行）

### 步骤
1. 重写 `analysis.ts`：
   - system prompt：角色设定从 CBT 治疗师 → 使用 `HEALING_PERSONA`
   - 输出格式从 JSON → 纯文本 Markdown（共情→洞察→行动 三段式）
   - 思维模式命名全部用大白话
   - 星象关联用生活化比喻
   - 新增「身体感受」字段的解读（如用户填写了 bodySignal）
2. 更新 `user` 函数以传入新字段（多情绪、bodySignal）
3. 确保输出长度控制在 200-400 字

### 验证
- [ ] Prompt 不含 CBT 专业术语（检查：灾难化、认知扭曲、自动思维、核心信念）
- [ ] 输出为可直接渲染的 Markdown 文本
- [ ] 共情段在前、行动建议具体可执行
- [ ] 安全边界（`CBT_SAFETY`）仍然生效

---

## Task 5: 统一其他 5 个 CBT Prompt 的语气
**状态**：待执行
**依赖**：Task 4

### 步骤
1. 逐个检查 `aggregate-analysis.ts`、`somatic-analysis.ts`、`root-analysis.ts`、`mood-analysis.ts`、`competence-analysis.ts`
2. 统一使用 `HEALING_PERSONA` 角色设定
3. 移除 CBT 专业术语，替换为大白话
4. 确保输出格式统一为 Markdown

### 验证
- [ ] 6 个 CBT Prompt 风格一致
- [ ] 无 CBT 专业术语残留
- [ ] 注册表正常加载

---

## Task 6: 后端 API 适配
**状态**：待执行
**依赖**：Task 4

### 步骤
1. 在 `cbt.ts` 的 `CBTRecord` 接口添加 `bodySignal?: string`
2. `/analysis` 端点的 context 组装中，将 `bodySignal` 传入 Prompt
3. 确保响应格式兼容（`content` 字段现在是 Markdown 文本）
4. 旧记录读取时 `bodySignal` 缺失不报错

### 验证
- [ ] POST `/api/cbt/analysis` 带 `bodySignal` 字段正常响应
- [ ] POST `/api/cbt/analysis` 不带 `bodySignal` 字段也正常响应
- [ ] GET `/api/cbt/records` 旧记录正常返回

---

## Task 7: 前端分析结果渲染适配
**状态**：待执行
**依赖**：Task 2, Task 4

### 步骤
1. 修改 `cbt.wxml` 分析结果区域，将 `{{analysis}}` 作为 Markdown 文本渲染
2. 由于小程序不原生支持 Markdown，使用 `<rich-text>` 或简单的文本分段渲染
3. 确保换行、加粗等格式正确显示

### 验证
- [ ] AI 分析结果正常显示
- [ ] 文本中的换行和段落格式正确
- [ ] 加载状态和错误状态正常

---

## Task 8: 文案细节打磨与整体测试
**状态**：待执行
**依赖**：Task 1-7 全部完成

### 步骤
1. 全面检查所有中文文案，确保口语化、年轻化
2. 检查空状态、错误提示、加载提示的文案
3. 端到端测试完整流程：记录 → 分析 → 查看历史
4. 检查日历情绪点颜色是否正确
5. 确保旧记录兼容显示

### 验证
- [ ] 完整流程无报错
- [ ] 所有文案无英文残留或 CBT 专业术语
- [ ] 日历、历史列表正常显示
- [ ] 旧格式记录不崩溃
