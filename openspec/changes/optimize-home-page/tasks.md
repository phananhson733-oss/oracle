# Tasks: 首页优化与个性化推荐系统

## 1. 后端 API 开发

- [ ] 1.1 实现用户状态查询接口 `GET /api/user/status`
  - 返回 isNewUser、hasBirthChart、hasUsedSynastry、lastCBTEntry、recentActions
  - 验证：curl 测试返回正确 JSON 结构

- [ ] 1.2 实现星象事件查询接口 `GET /api/astro/events`
  - 支持 date 参数查询
  - 返回当前有效的星象事件列表
  - 验证：curl 测试返回正确 JSON 结构

- [ ] 1.3 添加星象事件数据源
  - 创建星象事件数据表或配置文件
  - 预填充 2025-2026 年重要星象事件（水逆、新月、满月等）
  - 验证：数据库/配置文件包含有效数据

## 2. 前端布局重构

- [ ] 2.1 重构首页 WXML 结构
  - 保留 header 和 daily-card
  - 新增「快速体验」三列入口区域
  - 新增「我的星盘」模块
  - 重构「为你推荐」模块结构
  - 验证：页面结构符合设计稿

- [ ] 2.2 更新首页 WXSS 样式
  - 快速体验区域样式（三列等宽布局）
  - 我的星盘模块样式（列表项 + 箭头）
  - 推荐卡片新样式（支持 emoji 图标）
  - 验证：视觉效果符合设计规范

- [ ] 2.3 添加新入口的路由跳转
  - CBT日记入口 → `/pages/cbt/cbt`
  - AI问答入口 → `/pages/ask/ask`
  - 人生k线图入口 → 对应页面
  - 验证：点击各入口正确跳转

## 3. 推荐系统实现

- [ ] 3.1 实现用户状态获取逻辑
  - 新增 `fetchUserStatus()` 方法
  - 调用后端 API 获取用户状态
  - 错误处理：API 失败时使用默认值
  - 验证：控制台打印正确的用户状态

- [ ] 3.2 实现星象事件获取与缓存
  - 新增 `fetchAstroEvents()` 方法
  - 实现 24 小时本地缓存策略
  - 缓存 key: `astro_events_cache`
  - 验证：首次请求后，24 小时内不再请求

- [ ] 3.3 实现推荐优先级算法
  - 新增 `generateRecommendations()` 方法
  - 实现 8 种推荐类型的优先级计算
  - 返回排序后的前 3-4 个推荐项
  - 验证：不同用户状态返回不同推荐

- [ ] 3.4 实现新用户引导逻辑
  - 判断注册时间 ≤3 天
  - 新用户返回固定引导卡片
  - 验证：模拟新用户状态，显示引导内容

## 4. 推荐内容配置

- [ ] 4.1 创建推荐内容池配置
  - 功能引导类内容（5 条）
  - 星象提醒类内容模板
  - 热门/教育类内容（各 3 条）
  - 验证：配置文件结构正确

- [ ] 4.2 实现推荐卡片渲染
  - 支持 emoji 图标显示
  - 支持不同类型卡片样式
  - 支持点击跳转对应功能
  - 验证：各类型卡片正确渲染

## 5. 降级与容错

- [ ] 5.1 实现 API 降级策略
  - 用户状态 API 失败 → 使用本地存储推断
  - 星象事件 API 失败 → 使用缓存或跳过
  - 验证：断网情况下页面正常显示

- [ ] 5.2 实现加载状态
  - 推荐区域骨架屏
  - 加载失败提示
  - 验证：加载过程有视觉反馈

## 6. 集成测试

- [ ] 6.1 新用户场景测试
  - 模拟新用户状态
  - 验证显示引导卡片
  - 验证引导卡片点击跳转

- [ ] 6.2 老用户场景测试
  - 模拟已有本命盘用户
  - 验证显示个性化推荐
  - 验证推荐优先级正确

- [ ] 6.3 星象提醒场景测试
  - 模拟水逆期间
  - 验证显示星象提醒卡片
  - 验证提醒内容正确

## 依赖关系

```
1.1, 1.2, 1.3 (后端) → 可并行
2.1, 2.2, 2.3 (布局) → 可并行，不依赖后端
3.1 → 依赖 1.1
3.2 → 依赖 1.2
3.3 → 依赖 3.1, 3.2
3.4 → 依赖 3.1
4.1 → 不依赖
4.2 → 依赖 2.1, 3.3
5.1 → 依赖 3.1, 3.2
5.2 → 依赖 2.1
6.x → 依赖所有前置任务
```
