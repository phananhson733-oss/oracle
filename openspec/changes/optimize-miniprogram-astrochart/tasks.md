# 任务清单：优化小程序星盘组件

## 阶段一：单盘优化

### 1.1 更新几何常量
- [x] 在 `chart-config.js` 中添加与 Web 版本对齐的几何常量
- [x] 定义单盘布局参数（SINGLE_CHART_LAYOUT）
- [x] 定义双盘布局参数（DUAL_CHART_LAYOUT）
- **验证**：常量值与 Web 版本比例一致 ✓

### 1.2 实现行星位置信息显示
- [x] 修改 `drawPlanetInfo()` 方法，添加度数、星座、角分、逆行标记
- [x] 实现径向排列布局（从外到内：符号 → 度数° → 星座 → 分' → R）
- [x] 添加偏移连接线（当 visualAngle 与 absAngle 差异 > 0.5° 时）
- [x] 处理文字旋转，保持水平可读
- **验证**：单盘显示完整行星信息，与参考图一致 ✓

### 1.3 添加宫头标注
- [x] 新增 `drawHouseCuspLabels()` 方法
- [x] 在星座环上绘制宫头信息（度数 + 星座符号 + 分）
- [x] 处理文字方向（根据角度自动调整）
- **验证**：12个宫头标注正确显示 ✓

### 1.4 优化黄道带轮盘
- [x] 调整星座符号大小和位置
- [x] 优化星座环宽度比例
- [x] 统一颜色配置
- **验证**：星座环视觉效果与参考图一致 ✓

## 阶段二：双盘优化

### 2.1 实现内外环分离布局
- [x] 修改 `drawChart()` 方法，区分单盘/双盘布局
- [x] 实现外环行星位置信息显示
- [x] 实现内环行星位置信息显示
- [x] 添加内外环分隔线（`drawSeparator()` 方法）
- **验证**：双盘内外环清晰分离 ✓

### 2.2 优化跨盘相位线
- [x] 修改 `drawAspects()` 方法，正确处理跨盘相位
- [x] 实现三层分层渲染（foreground/midground/background）
- [x] 优化相位线颜色和透明度
- **验证**：跨盘相位线正确绘制，分层效果明显 ✓

### 2.3 调整双盘字号
- [x] 外环字号缩放 90%（在 DUAL_CHART_LAYOUT.fontSize 中配置）
- [x] 内环字号缩放 90%
- [x] 调整元素间距（在 DUAL_CHART_LAYOUT.spacing 中配置）
- **验证**：双盘信息清晰可读，不拥挤 ✓

## 阶段三：行星详情卡片优化

### 3.1 增强行星详情卡片
- [x] 参考 Web 版本 `PlanetTooltip.tsx` 设计小程序版本
- [x] 显示行星符号、名称、逆行/顺行状态
- [x] 显示星座位置（星座名 + 符号 + 度数）
- [x] 显示宫位信息（所在宫位 + 守护宫位）
- [x] 显示相关相位列表（最多5个，按 orb 排序）
- **验证**：点击行星显示完整详情卡片 ✓

### 3.2 相位信息展示
- [x] 显示相位类型符号（☌ ☍ □ △ ⚹）
- [x] 显示相位角度和容许度
- [x] 区分跨盘相位和同盘相位（双轮盘）
- [x] 相位颜色与星盘一致
- **验证**：相位信息清晰准确 ✓

### 3.3 卡片交互优化
- [x] 优化点击区域计算（扩大触摸区域至 15px）
- [x] 卡片位置自适应（避免超出屏幕）
- [x] 点击空白处关闭卡片
- [x] 添加关闭按钮
- **验证**：交互流畅，无误触 ✓

## 阶段四：测试与调优

### 4.1 页面集成测试
- [ ] 测试「本我」页面（self）- 本命盘
- [ ] 测试「今日运势」页面（daily）- 行运盘
- [ ] 测试「专业排盘」页面（chart）- 多种盘型
- [ ] 测试「发现」页面（discovery）- 合盘
- **验证**：所有页面星盘显示正常

### 4.2 尺寸适配测试
- [ ] 测试 350px 尺寸（默认）
- [ ] 测试全屏尺寸（isZoomed 模式）
- [ ] 测试不同设备 DPR
- **验证**：各尺寸下显示正确，无模糊

### 4.3 交互功能测试
- [ ] 测试行星点击详情卡片
- [ ] 测试点击区域计算
- [ ] 测试卡片位置自适应
- **验证**：点击交互正常工作

### 4.4 性能优化
- [ ] 检查绘制性能（避免频繁重绘）
- [ ] 优化 Canvas 操作（批量绘制）
- [ ] 测试滚动流畅度
- **验证**：页面滚动流畅，无卡顿

## 依赖关系

```
1.1 几何常量 ──┬──> 1.2 行星信息
              ├──> 1.3 宫头标注
              └──> 1.4 黄道带轮盘
                        │
                        v
              2.1 内外环分离 ──> 2.2 跨盘相位 ──> 2.3 双盘字号
                        │
                        v
              3.1 详情卡片 ──> 3.2 相位展示 ──> 3.3 交互优化
                                                      │
                                                      v
                                              4.1-4.4 测试与调优
```

## 可并行任务

- 1.2、1.3、1.4 可并行开发（依赖 1.1 完成后）✓ 已完成
- 3.1、3.2、3.3 可并行开发（依赖阶段二完成后）✓ 已完成
- 4.1、4.2、4.3 可并行测试（依赖阶段三完成后）

## 实现总结

### 修改的文件

1. **`astromind/miniprogram/constants/chart-config.js`**
   - 添加 `SINGLE_CHART_LAYOUT` 和 `DUAL_CHART_LAYOUT` 几何常量
   - 添加 `ASPECT_SYMBOLS` 相位符号映射
   - 添加 `PLANET_NAMES_ZH` 和 `SIGN_NAMES_ZH` 中文名称映射
   - 添加 `PLANET_KEYWORDS` 行星关键词
   - 添加 `SIGN_RULERS` 星座守护星映射

2. **`astromind/miniprogram/components/astro-chart/astro-chart.js`**
   - 重写 `drawChart()` 方法，支持单盘/双盘布局切换
   - 重写 `drawZodiacWheel()` 方法，使用布局配置
   - 重写 `drawHouseCusps()` 方法，添加宫位数字和结构圆
   - 新增 `drawHouseCuspLabels()` 方法，绘制宫头标注
   - 新增 `drawSeparator()` 方法，绘制双盘分隔线
   - 重写 `drawAspects()` 和 `drawAspectLine()` 方法
   - 新增 `drawPlanetInfo()` 方法，替代原 `drawPlanets()`
   - 增强 `showPlanetDetail()` 方法，显示完整详情
   - 新增 `getRelevantAspects()` 和 `getRuledHouses()` 辅助方法

3. **`astromind/miniprogram/components/astro-chart/astro-chart.wxml`**
   - 重新设计详情卡片结构，支持完整信息展示

4. **`astromind/miniprogram/components/astro-chart/astro-chart.wxss`**
   - 重新设计详情卡片样式，与 Web 版本风格一致
